<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../../partials/head'); %>
</head>

<body>

    <%- include('../../partials/header'); %>

        <main class="container">

            <div class="card">
                <h1 class="card-header">
                    Add Concentration
                </h1>
                <div class="card-body">
                    <form action="/concentrations/add" method="post">
                        <div class="form-group">
                            <label class="my-1 mr-2" for="object_id">Object name</label>
                            <select class="custom-select my-1 mr-sm-2 form-control" id="object_id" name="object_id">
                                <option selected>Choose...</option>
                                <% objects.forEach(function(object) { %>
                                    <option value="<%= object.object_id %>">
                                        <%= object.name %>
                                    </option>
                                    <% }); %>
                            </select>
                        </div>
                        <div class="form-group">
                            <label class="my-1 mr-2" for="pollutant_code">Pollutant name</label>
                            <select class="custom-select my-1 mr-sm-2 form-control" id="pollutant_code" name="pollutant_code">
                                <option selected>Choose...</option>
                                <% pollutants.forEach(function(pollutant) { %>
                                    <option value="<%= pollutant.pollutant_code %>">
                                        <%= pollutant.pollutant_name %>
                                    </option>
                                    <% }); %>
                            </select>
                        </div>
                        <div class="form-group">
                            <label>Concentration value:</label>
                            <input type="number" min="0" max="9999" step="any" class="form-control" name="concentration_value" value="<%= concentration_value %>" autocomplete="off">
                        </div>
                        <div class="form-group">
                            <input type="submit" class="btn btn-info mt-3" value="Add" />
                        </div>
                    </form>
                </div>
            </div>
        </main>

        <%- include('../../partials/footer'); %>

</body>

</html>